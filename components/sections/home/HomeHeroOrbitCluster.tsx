/**
 * Hero orbit cluster — **empty shell** while the illustration is rebuilt step by step
 * (orbits, centre, satellites). Keeps a stable slot in the 2-col layout.
 */
export function HomeHeroOrbitCluster() {
  return (
    <div
      className="home-hero-orbit-cluster mx-auto w-full max-w-[min(100%,20rem)] select-none lg:max-w-[22.5rem]"
      aria-hidden
    />
  );
}
